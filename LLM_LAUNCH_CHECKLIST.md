# LLM 종합 답변 — 실제 결제 전환 체크리스트

`LLM_SYNTHESIS_ENABLED`/`ANTHROPIC_API_KEY`는 지금 의도적으로 비활성 상태입니다.
이 문서는 "실제로 서비스를 진행한다"고 결정해 Anthropic API에 실제로 돈을 충전하는
시점에 처리해야 할 작업을 정리합니다. 코드 자체는 이미 opt-in 안전 폴백 구조라 키가
없어도 기존 규칙 기반 결과는 정상 동작합니다(`README.md`의 "LLM 종합 답변" 절 참고).

## 1. Anthropic Console 결제 설정

- [ ] console.anthropic.com에 조직 생성/확인 (claude.ai Pro/Max 구독과는 별개 계정·과금 체계).
- [ ] 결제수단 등록 또는 선불 크레딧 충전.
- [ ] Console의 사용량 한도(spend limit)/예산 알림 기능으로 월 상한을 걸어 예상치 못한 폭증 방지.
- [ ] 이 서비스 전용 API 키를 별도로 발급 (다른 프로젝트와 키를 공유하지 않아 유출/폭주 시 영향 범위를 격리).

## 2. 시크릿 관리

- [ ] `ANTHROPIC_API_KEY`, `LLM_SYNTHESIS_ENABLED=true`를 배포 환경(서버/컨테이너/시크릿 매니저)에
      안전하게 주입 — 저장소에 커밋하지 않음, 로그에 남기지 않음.
- [ ] `.env`가 `.gitignore`에 포함되어 있는지 재확인.
- [ ] 키 로테이션 절차를 정해둔다(유출 의심 시 즉시 재발급 가능하도록).

## 3. 라이브 종단 검증 (키 활성화 직후 1회 필수)

현재까지는 `ANTHROPIC_API_KEY`가 없어 Citations API 실제 호출을 라이브로 검증하지
못한 상태입니다(`PROGRESS.md` "LLM 종합 답변" 절의 제약 사항 참고). 키를 넣은 직후
아래를 반드시 확인합니다.

- [ ] `legalCitationSynthesis.service.ts` → Citations API 실제 호출 성공 확인 (요청 스키마는
      문서로 검증됐으나 실제 응답 형태는 아직 미검증).
- [ ] `legalAnswerSynthesis.service.ts`의 전체 파이프라인(생성 → `verify_citations` 재검증)이
      실제 API 응답으로도 정상 동작하는지 확인.
- [ ] 의도적으로 존재하지 않는 조문을 유도하는 질문으로 2차 안전장치가 실제 환각을 잡아내는지
      재확인 (mock 응답으로는 이미 확인했으나 실제 Claude 응답 형태로는 미확인).
- [ ] API 에러/타임아웃/레이트리밋 상황에서 `used:false` 폴백으로 안전하게 떨어지는지 확인.
- [ ] 브라우저에서 "AI 종합 답변 (실험적)" 섹션이 실제로 채워지는 것을 확인.

## 4. 운영 중 모니터링

- [ ] Console 사용량 대시보드를 정기적으로 확인할 담당자/주기를 정한다.
- [ ] 요청당 input/output 토큰 수를 로그에 남겨 실제 비용을 API 호출 건수와 함께 추적 가능하게 한다.
- [ ] 크레딧 잔액이 임계치 이하로 떨어졌을 때 알림받을 방법(Console 알림 기능 확인) 설정.

## 5. 법률 서비스 특유의 확인 사항

- [ ] Anthropic 사용 정책상 법률 정보 제공 서비스로 문제가 없는지 재확인.
- [ ] LLM 답변에도 "최종 법률 자문 아님" 등 기존 정책 문구(`policy.disclaimers`)가 항상 함께
      노출되는지 확인 — 지금 구조는 워크플로 응답에 자동으로 포함되므로 새 코드를 추가하지
      않는 한 유지될 것이나, LLM 답변 UI를 바꿀 경우 재확인 필요.

## 6. 이번 결정과 관련해 보류한 대안

- OpenRouter 크레딧으로 전환하는 방안은 검토 후 기각. Anthropic Citations API(1차
  안전장치)를 OpenRouter의 OpenAI 호환 API가 지원하지 않아 안전장치가 약화되는 반면,
  토큰당 가격은 Anthropic 직접과 동일(OpenRouter는 마크업 없음, 크레딧 충전 시에만
  5~5.5% 수수료)해서 비용 이점이 크지 않았음. 상세 비교는 대화 기록 참고, 필요하면
  이 문서에 요약을 추가할 것.

## 다음 단계 후보 (README/PROGRESS.md에 이미 기록됨, 참고용)

- 판례 전문 조회(`get_precedent_text`) 후 판례도 Citations 문서로 포함.
- 다른 워크플로(계약서 검토·문서 초안·소송 준비)로 LLM 종합 답변 확장.
- `verify_citations`의 "확인필요(⚠)" 상태가 자연스러운 문장에서 과다 발생하는 문제 완화 검토.
